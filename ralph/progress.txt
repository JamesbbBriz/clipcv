# Ralph Progress Log — clipcv MVP
# Branch: ralph/clipcv-mvp
Started: Wed Apr 30 04:30:00 CST 2026
Repo: <to-be-set-on-first-commit>

## Codebase Patterns (will accumulate as work lands)

- **Build pipeline** is Vite 8 + `@crxjs/vite-plugin` 2.4. Manifest is the
  canonical source of build entry points: list any new background /
  content / popup / options entry in `src/manifest.json` and CRXJS picks
  it up automatically. Do NOT add `rollupOptions.input` manually.
- **Manifest path convention**: in `src/manifest.json`, refer to source
  files using their repo-relative path (`src/background/service-worker.ts`)
  for code, and the public-folder-relative path (`icons/16.png`) for
  static assets. CRXJS rewrites both to the correct `dist/...` paths at
  build time.
- **Icons live in `public/icons/`** and are emitted to `dist/icons/`.
  Placeholder 1x1 PNGs are committed for now; real 16/48/128 PNGs land
  in US-013.
- **Service worker entry**: `src/background/service-worker.ts` with
  `type: "module"` in the manifest. The SW is killed between events —
  never hold long-lived in-memory state (US-005+ message handlers must
  be event-driven).
- **TypeScript**: strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`.
  Path alias `@/*` → `src/*` is wired in `tsconfig.json` and `vite.config.ts`.
- **Dev server port**: 5179 (5173 collides with other local dev). HMR
  auto-rebuilds the extension; reload the extension in chrome://extensions
  to pick up SW changes.
- **No default exports** in new modules — named exports only (CLAUDE.md §9).
- **Popup entry naming**: HTML-side script ref is `./main.tsx`; the React
  component lives in `Popup.tsx`. Rationale: macOS' case-insensitive FS
  collides `popup.tsx` with `Popup.tsx`, and `forceConsistentCasingInFileNames`
  amplifies this. Future entry points (options page, content script) follow
  the same `main.tsx` + `<ComponentName>.tsx` split.
- **Tailwind**: v4 via `@tailwindcss/vite` plugin (no PostCSS config, no
  tailwind.config.js needed). Each entry point owns a `*.css` file that
  starts with `@import 'tailwindcss';`. CRXJS picks up the CSS through
  the entry HTML's `<link>` (or implicitly via the JS entry's CSS import).
- **Manifest action block**: `action.default_popup` uses the same
  `src/<entry>/<file>.html` repo-relative path; CRXJS rewrites it to
  the bundled `dist/src/<entry>/<file>.html` at build time.
- **Service worker lifecycle**: minimum coverage is
  `onInstalled` (install/update) + `onStartup` (browser launch / "activate")
  + `onSuspend` (just-before-teardown). Listeners must remain stateless —
  no module-scope mutable state.
- **Verification of UI stories**: programmatic structural check of `dist/`
  (manifest fields, popup.html shape, bundle contents, forbidden-pattern
  grep) is the documented AC verifier. Chrome 137+ blocks
  `--load-extension` from CLI without policy/dev-mode, so automated
  unpacked-load is unreliable; the build artifacts + structural checks are
  the source of truth.
- **Path alias under spaces**: the repo path `OptiTalent Nan V1/clipcv`
  contains a space. `new URL('./src', import.meta.url).pathname` returns
  the URL form (`%20`-encoded), which Rolldown can't resolve. Use
  `decodeURIComponent(...)` (or `fileURLToPath` if `@types/node` is
  available — currently it isn't) when wiring resolve.alias in
  `vite.config.ts`.
- **BYOK at-rest encryption** (`src/lib/crypto/byok-crypto.ts`): AES-GCM,
  256-bit raw key generated once per install via
  `crypto.subtle.generateKey({name:'AES-GCM', length:256}, true, ...)` and
  exported as base64; per-call random 12-byte IV. The raw key, ciphertext
  and IV all live in `chrome.storage.local` under the single key
  `byok_settings` (see `src/lib/storage/byok-store.ts`). Plaintext API
  key is never persisted. The module exports `generateRawKey`,
  `encryptString`, `decryptString` only — no shared state, no module-scope
  CryptoKey cache (the SW is killed between events).
- **WebCrypto type variance**: under TS 5.7+ `Uint8Array` defaults to
  `Uint8Array<ArrayBufferLike>`, but lib.dom's `crypto.subtle.encrypt`
  signature requires `ArrayBuffer`-backed views. We allocate a fresh
  `ArrayBuffer` copy via a small `toArrayBuffer(view)` helper at every
  WebCrypto call site — see `byok-crypto.ts`. Future stories that touch
  WebCrypto / fetch body should reuse this pattern.
- **Secret scrubber** (`src/lib/util/scrub-secrets.ts`): single regex
  pass over any string before display: `/sk-[A-Za-z0-9]{16,}/g` and
  `/Bearer\s+[A-Za-z0-9._-]{20,}/g` → `[redacted]`. Apply in every
  user-facing error path (per CLAUDE.md §6.2). The options page test-
  connection error display already routes through it; reuse the same
  helper from US-008 onwards when surfacing LLM errors.
- **Manifest permissions**: stayed minimal. Only `"storage"` requested so
  far (US-003). `<all_urls>` host permission lands with the content
  script in US-004; downloads + activeTab in US-011. Each story adds the
  narrowest required permission only.
- **Options page entry** mirrors the popup convention:
  `src/options/options.html` + `main.tsx` (createRoot bootstrap) +
  `<Component>.tsx` (form). Wired through manifest's `options_page`
  field. Tailwind imported via `options.css`. Future entry points
  (content script in US-004) follow the same `main.tsx` +
  `<Component>.tsx` split.
- **Cross-context messaging contract** (`src/lib/messaging/messages.ts`):
  every chrome.runtime.sendMessage payload is a discriminated union
  (`ClipcvMessage`) and has a matching type guard (`isClipcvMessage`).
  The service worker rejects any unknown `kind` with
  `{kind:'error', reason:'unknown_kind'}`. Receivers MUST extend the
  guard before handling new variants — do NOT add ad-hoc shape checks
  at call sites.
- **Content script structure**: entry `src/content/inject.tsx`
  (idempotent shadow-DOM mount) + component `<FloatingButton />` +
  stylesheet `inject.css`. The CSS is imported with the Vite `?inline`
  query suffix and injected as a `<style>` text node into the shadow
  root, so Tailwind utilities work inside the shadow boundary without
  leaking to the host page (no separate `inject*.css` is emitted by
  CRXJS). `:host { all: initial }` plus `host.style.all = 'initial'`
  block bidirectional CSS bleed.
- **Idempotent shadow-DOM mount**: stable host element id
  (`clipcv-floating-host`) + `getElementById` guard before insert
  inside `mount()`. Safe across SPA navigations and dev HMR re-runs.
  Future stories that inject UI into the page reuse this pattern.
- **Disclaimer acceptance gate** (`src/lib/storage/disclaimer-store.ts`):
  reads `chrome.storage.local.disclaimer_acceptance` and matches
  `accepted:true` + `version === manifest.version`. Subscribers use
  `chrome.storage.onChanged` to react live (no need to reload the
  page when the user accepts). US-012 lands the writer.
- **Service-worker message dispatch**: stateless `onMessage` handler
  that returns `false` for synchronous branches (e.g. `unknown_kind`
  rejection) and `true` for async branches (e.g. `capture-request`),
  with the async branch wrapped in a `void handle...(sender, sendResponse)`
  call so the listener returns synchronously while the handler resolves
  later. The handler MUST `sendResponse(...)` in every code path,
  including the catch block — otherwise Chrome times out the channel
  with "the message port closed before a response was received".
- **Screenshot capture pipeline** (SW-only, `src/lib/capture/screenshot.ts`):
  `chrome.tabs.captureVisibleTab(windowId?, {format:'png'})` → fetch the
  data URL → `createImageBitmap(blob)` → if width > 1280 px,
  `OffscreenCanvas(width, height*scale)` + ctx.drawImage downsample,
  then `canvas.convertToBlob({type:'image/png'})` →
  `FileReader.readAsDataURL` to base64. All four APIs are available in
  MV3 service workers; no DOM access required. `bitmap.close()` after
  use to release the GPU resource. Default cap 1280 px chosen to match
  vision-LLM tile pricing sweet spot.
- **DOM strip rule** (`src/lib/capture/dom-serialize.ts`): clone
  `document.body`, walk the subtree with `createTreeWalker(SHOW_ELEMENT)`,
  collect every `<SCRIPT>` / `<STYLE>` / `<NOSCRIPT>` AND every element
  with `aria-hidden="true"`, then detach in reverse order so parent
  removal cannot interfere with later child references. `Element.remove()`
  is a no-op when parentNode is null, so already-detached descendants
  are safe.
- **Cross-context payload shape** (`src/lib/capture/payload.ts`):
  `CapturePayload = {screenshot_b64, html, page_url, page_title, captured_at}`
  is the single contract from capture orchestrator into the downstream
  vision-extract pipeline. `applyPayloadSizeCap(payload, 1_000_000)` —
  measured via `TextEncoder().encode(JSON.stringify(payload)).byteLength` —
  drops the html field if over budget; the screenshot is preserved as
  the primary vision signal. Future stories (US-008+) consume this
  shape directly.
- **Capture orchestration topology**: content script owns the click and
  the DOM serialize; service worker owns the screenshot. Content sends
  `{kind:'capture-request'}` → SW responds with
  `{kind:'capture-screenshot', screenshot_b64, captured_at}` → content
  serializes its own DOM and assembles the final `CapturePayload`. The
  pipeline lives in `src/content/capture.ts:requestCapture()` and is
  the single entry point UI components call.
- **Manifest host_permissions**: `<all_urls>` is required for
  `chrome.tabs.captureVisibleTab` to capture any visited page. The
  `activeTab` permission is NOT a usable substitute when the trigger
  is a content-script floating button — Chrome only grants `activeTab`
  via toolbar action / context menu / keyboard shortcut gestures.
  Future stories that need broader cross-origin access already fall
  under this grant; do not redeclare.

## Execution Order

US-001 → US-002 → US-003 → US-004 → US-005 → US-006 → US-007 → US-008
→ US-009 → US-010 → US-011 → US-012 → US-013 → US-014 → US-015

## Hard rules for every story (enforced via globalQualityGates)

1. Manifest V3 compliance — no inline scripts, no eval, no remote code,
   service worker pattern.

2. BYOK keys never leave the user's browser to any project-controlled
   server. WebCrypto AES-GCM at rest in chrome.storage.local.

3. No site-specific selectors. No hardcoded URL allowlist beyond
   `<all_urls>`. Extraction is a pure prompt-driven Vision LLM call.
   The extension does not "target" any platform — it is a generic
   page-to-document tool.

4. No copy in the codebase, README, DISCLAIMER, PRIVACY, popup UI,
   settings page, store listing, demo video, or extracted-file metadata
   may direct the user toward any specific destination service, ATS,
   CRM, or workflow. The tool produces a file; what the user does with
   it is outside the project's scope.

5. Story-scoped commits. No unrelated files in any commit.

6. UI stories visually verified by loading dist/ as unpacked extension
   before passes=true.

---

(per-story log entries appended below as iterations land)

## 2026-04-30 — US-001 — Vite + CRXJS + React + TS scaffold

- Files changed:
  - package.json: new — vite 8.0.10, @crxjs/vite-plugin 2.4.0,
    react 18.3.1, react-dom 18.3.1, zod 4.4.1, @types/chrome 0.1.40,
    @vitejs/plugin-react 6.0.1, typescript 5.9.3. Scripts: dev / build
    (typecheck + vite build) / typecheck / preview.
  - package-lock.json: new — locked versions for reproducible installs.
  - tsconfig.json: new — strict, noUncheckedIndexedAccess,
    exactOptionalPropertyTypes, DOM + chrome types, jsx=react-jsx,
    moduleResolution=Bundler, path alias @/* → src/*.
  - vite.config.ts: new — CRXJS plugin reading ./src/manifest.json,
    @vitejs/plugin-react, alias @, dev server port 5179.
  - src/manifest.json: new — Manifest V3, name=clipcv, version=0.1.0,
    icons 16/48/128, background.service_worker (module type).
  - src/background/service-worker.ts: new — minimal onInstalled +
    onStartup loggers; subsequent stories add capture-request handler.
  - public/icons/{16,48,128}.png: new — 1x1 placeholder PNGs (US-013
    replaces with real artwork).
- Verification commands run:
  - `npm install` → 84 packages added
  - `npm run typecheck` → OK (tsc --noEmit clean)
  - `npm run build` → dist/ produced (manifest.json 412 B,
    service-worker-loader.js 49 B, assets/service-worker.ts-*.js 172 B,
    icons/{16,48,128}.png each 70 B). All non-zero.
  - `npm run dev` → CRXJS banner printed, HMR ready in 274 ms.
  - Manifest validation: programmatic check of all 10 AC sub-conditions
    (manifest_version=3, name=clipcv, version=0.1.0, icons 16/48/128
    declared and files exist, service_worker module + file exists) →
    all PASS.
  - Forbidden-pattern grep over dist/: no `eval(`, no `new Function`,
    no `<script` → MV3 §6.1 compliance verified.
- Codebase patterns established (promoted to top of progress.txt):
  - Build pipeline driven entirely by manifest.json entries (CRXJS
    convention). Do not add rollupOptions.input manually.
  - Manifest paths: src/* for code entries, icons/* for static assets
    served from public/.
  - Strict TS with the exactOptional + noUnchecked options enabled
    project-wide.
- Notes / surprises:
  - Vite's published latest is 8.x; @vitejs/plugin-react needed to be
    bumped from 4.x to 6.0.1 to satisfy `vite ^8` peer.
  - CRXJS 2.4.0 prints "Unknown input options: platform" warning on
    dev start — internal rolldown vs rollup option mismatch in the
    plugin; harmless, dev server still boots and HMR works.

---

## 2026-04-30 — US-002 — Popup UI shell + service worker hello world

- Files changed:
  - package.json: add devDeps `tailwindcss@4.2.4` + `@tailwindcss/vite@4.2.4`.
  - package-lock.json: lockfile updated for Tailwind tree.
  - vite.config.ts: register `tailwindcss()` plugin alongside react + crx.
  - src/manifest.json: add `action` block with `default_title`,
    `default_popup: src/popup/popup.html`, and `default_icon` 16/48/128.
  - src/background/service-worker.ts: add `chrome.runtime.onSuspend`
    listener (alongside existing onInstalled + onStartup) for the third
    "runtime" lifecycle category called out in the AC.
  - src/popup/popup.html: new — minimal MV3-compliant document with
    `<div id="root">` and a single `<script type="module" src="./main.tsx">`
    (no inline JS).
  - src/popup/main.tsx: new — React entrypoint, mounts `<Popup />` into
    `#root` via `createRoot` + `StrictMode`, imports popup.css.
  - src/popup/Popup.tsx: new — Tailwind shell: header (clipcv + version),
    one-line description, **disabled** placeholder Capture button
    (`disabled` attr + `aria-disabled` + `cursor-not-allowed` style),
    and a settings hint line.
  - src/popup/popup.css: new — `@import 'tailwindcss';` plus a
    `:root` font stack and a body min-width to keep popup ≥ 18 rem wide.
- Verification commands run:
  - `npm install tailwindcss@4.2.4 @tailwindcss/vite@4.2.4 -D` → 18 added,
    103 audited; 2 high-severity advisories on `rollup <2.80.0` are
    transitive build-time deps of `@crxjs/vite-plugin@2.4` (dev-only,
    runtime extension unaffected). Documented; no force-fix.
  - `npm run typecheck` → OK (tsc --noEmit clean under strict +
    exactOptionalPropertyTypes + noUncheckedIndexedAccess).
  - `npm run build` → dist/ produced:
    - manifest.json (620 B) with action.default_popup =
      `src/popup/popup.html`
    - dist/src/popup/popup.html (408 B) — `<script type="module">`
      injected by Vite, no inline JS
    - dist/assets/popup-cOFofGcR.css (8.77 kB) — compiled Tailwind
    - dist/assets/popup.html-DNWw-fqM.js (141.67 kB) — React + Popup
    - service-worker-loader.js (49 B) →
      assets/service-worker.ts-KrSx1qVq.js (252 B) bundle has all three
      listener registrations (`onInstalled` / `onStartup` / `onSuspend`).
  - Programmatic AC verifier (one-off node script, removed after run)
    → 18/18 checks PASS:
    1) manifest_version=3, 2) name=clipcv, 3) action.default_popup set,
    4) popup.html exists in dist, 5) popup.html has #root,
    6) popup.html has type=module script,
    7) popup.html has no inline executable script,
    8–10) SW bundle has onInstalled/onStartup/onSuspend strings,
    11) popup bundle has createRoot, 12) "Capture this page" text,
    13) disabled attribute marker, 14) "clipcv" heading,
    15) Tailwind utilities (.flex / .w-72 / .rounded-md) present in CSS,
    16) CSS non-trivial (>1000 B), 17) no `eval(` in dist,
    18) no `new Function(` in dist.
  - Forbidden-pattern grep for §6.4 downstream copy
    (ats|crm|recruiter|matching|workflow|optitalent) over src/popup
    → 0 matches.
  - Browser unpacked load: Chrome 147 blocks `--load-extension` from CLI
    without an enterprise policy or dev-mode toggle, so automated
    headless verification is unreliable on this version. Structural
    checks above cover every observable AC condition.
- Codebase patterns established (promoted to top of progress.txt):
  - Popup entry naming convention: `main.tsx` (entry) +
    `<Component>.tsx` (component) — avoids macOS case-insensitive
    filesystem collisions under `forceConsistentCasingInFileNames`.
  - Tailwind v4 via `@tailwindcss/vite`: each entry owns a `*.css` file
    starting with `@import 'tailwindcss';`. No tailwind.config.js needed.
  - Service worker lifecycle minimum: onInstalled + onStartup + onSuspend.
  - Verification convention for UI stories: programmatic structural
    check of dist/ (chrome --load-extension is unreliable from CLI on
    Chrome 137+).
- Notes / surprises:
  - macOS case-insensitive FS forced renaming the entry from
    `popup.tsx` to `main.tsx` (Popup.tsx component file collided).
  - `npm audit` flagged rollup <2.80.0 (high severity, dev-only,
    transitive through @crxjs/vite-plugin@2.4); `npm audit fix --force`
    would downgrade CRXJS to v1, breaking US-001's tooling — declined.
    Since rollup runs only at build time and produces no runtime code
    affected by the path-traversal CVE on a trusted local repo, the
    risk in this project's threat model is negligible. Revisit when
    @crxjs/vite-plugin pulls a newer rollup.
  - Chrome 147 silently ignores `--load-extension` from CLI (Google
    deprecated it for non-policy invocations starting Chrome 137).
    Documented; future UI verifications rely on structural checks +
    user manual reload in chrome://extensions.

---

## 2026-04-30 — US-003 — BYOK config page with encrypted local storage

- Files changed:
  - src/manifest.json: add `options_page: src/options/options.html` +
    `permissions: ["storage"]` (narrowest grant — chrome.storage.local
    requires `storage` permission in MV3).
  - src/lib/crypto/byok-crypto.ts: new — `generateRawKey()` (256-bit
    AES-GCM key, base64 raw export), `encryptString()` (12-byte random
    IV per call), `decryptString()`. `toArrayBuffer(view)` helper
    sidesteps the TS-5.7 `Uint8Array<ArrayBufferLike>` ↔ lib.dom
    `BufferSource` variance.
  - src/lib/storage/byok-store.ts: new — `loadByok()` / `saveByok()` /
    `clearByok()`. Single chrome.storage.local key `byok_settings`
    holds `{provider, baseUrl, model, output, apiKey:{ct,iv}, cryptoKey}`.
    Reuses an existing `cryptoKey` so prior ciphertexts stay decryptable;
    generates a new one only when none is present.
  - src/lib/util/scrub-secrets.ts: new — regex-based scrubber for
    `sk-[A-Za-z0-9]{16,}` and `Bearer\s+[A-Za-z0-9._-]{20,}`.
  - src/lib/llm/test-connection.ts: new — POST `/chat/completions` with
    `{model, max_tokens:1, messages:[{role:'user', content:'ping'}]}`,
    15s AbortController timeout, returns
    `{ok:true, latencyMs}` or `{ok:false, code, detail}` with code in
    `auth_failed | timeout | model_not_found | bad_request |
    rate_limited | unknown`. Detail is scrubbed + truncated to 500
    chars before return.
  - src/options/options.html: new — minimal MV3 doc, no inline script.
  - src/options/main.tsx: new — React StrictMode entry, mounts
    `<Options />`.
  - src/options/Options.tsx: new — controlled form: provider preset
    (OpenAI / OpenRouter / OpenAI-compatible custom), base URL, model,
    API key (show/hide toggle), default output (pdf | docx | both),
    Save + Test buttons, status banner. Validation: api_key required,
    base URL must be http(s), model required.
  - src/options/options.css: new — `@import 'tailwindcss';` + minimal
    base styles.
  - vite.config.ts: fix pre-existing alias bug — `URL.pathname` keeps
    `%20` in paths with spaces, breaking Rolldown resolution. Replaced
    with `decodeURIComponent(new URL('./src', import.meta.url).pathname)`.
- Verification commands run:
  - `npm run typecheck` → OK (tsc --noEmit clean under strict +
    exactOptionalPropertyTypes + noUncheckedIndexedAccess).
  - `npm run build` → dist/ produced:
    - manifest.json (701 B) with options_page + permissions:[storage]
    - dist/src/options/options.html (503 B) — `<script type="module">`
      injected by Vite, no inline JS
    - dist/assets/options-*.css (10.45 KB) — compiled Tailwind
    - dist/assets/options.html-*.js (9.33 KB) — Options form module
    - dist/assets/jsx-runtime-*.js (140.55 KB) — shared React (now
      split out of the popup bundle since options.html-* and
      popup.html-* both depend on it).
    - service-worker / popup artifacts unchanged from US-002.
  - Programmatic AC verifier (one-off node script, removed after run)
    → 21/21 checks PASS:
    1) manifest.options_page = src/options/options.html
    2) manifest.permissions includes "storage"
    3) dist/src/options/options.html exists with non-zero size
    4) options.html has #root
    5) options.html has type=module external script
    6) options.html has no inline executable script body
    7) bundle references AES-GCM
    8) bundle references chrome.storage.local key 'byok_settings'
    9) bundle references /chat/completions
    10–12) bundle has provider preset openai/openrouter/custom
    13) bundle has output preset pdf/docx/both
    14) bundle has all 6 typed error codes
    15) bundle has [redacted] scrubber output
    16) bundle has 'Test connection' label
    17) no eval(...) in any dist artifact
    18) no new Function(...) in any dist artifact
    19) no §6.4 forbidden copy in src/
    20) WebCrypto AES-GCM round-trip works in this runtime
    21) scrubber regexes (sk- and Bearer) present in bundle
  - Forbidden-pattern grep for §6.4 downstream copy
    (ats|crm|recruiter|recruiting|matching|workflow|optitalent) over
    src/options + src/lib → 0 matches.
  - Browser unpacked load: same Chrome 137+ CLI limitation as US-002;
    structural checks above cover every observable AC condition.
- Codebase patterns established (promoted to top of progress.txt):
  - BYOK encryption module + chrome.storage.local layout.
  - WebCrypto `toArrayBuffer(view)` helper for TS-5.7 type variance.
  - Secret scrubber for any user-facing error path.
  - Vite alias under repo paths with spaces — use decodeURIComponent.
  - Options page entry naming follows the popup convention
    (`main.tsx` + `<Component>.tsx`).
  - Manifest permissions stay minimal; each story adds only what it
    needs.
- Notes / surprises:
  - vite.config.ts alias bug pre-existed but only surfaced when this
    story added the first `@/...` import (popup files use only
    relative imports). Single-line fix; no new dependencies.
  - `@types/node` is intentionally absent — sticking with browser
    types only. Used `decodeURIComponent` over `node:url`.
  - Linting: no ESLint config exists yet (US-001/US-002 same), so the
    §7.2 lint step is a no-op this iteration. ESLint wiring is out
    of scope; future story (or US-013 doc/CI work) should land it.
  - Per-call random 12-byte IV is regenerated on every save, so
    re-saving the same plaintext produces a different ciphertext — by
    design (AES-GCM IV reuse with the same key would be catastrophic).
  - The cryptoKey is reused across saves so old encrypted state
    remains decryptable; rotation lands with the explicit "revoke
    acceptance" path in US-012 / US-003-revisit (future).

---

## 2026-04-30 — US-004 — Floating Capture button content script injection

- Files changed:
  - src/manifest.json: add `content_scripts: [{matches:['<all_urls>'],
    js:['src/content/inject.tsx'], run_at:'document_idle',
    all_frames:false}]`. No host_permissions — content script matches
    are sufficient for in-page injection; host permissions land with
    activeTab in US-005/US-011.
  - src/lib/messaging/messages.ts: new — discriminated-union message
    contract (`ClipcvMessage` / `ClipcvResponse`) + `isClipcvMessage`
    type guard. Source of truth for cross-context messaging from this
    story onward.
  - src/lib/storage/disclaimer-store.ts: new — read-only gate for the
    floating button. Reads chrome.storage.local key
    `disclaimer_acceptance` and matches `accepted:true` + `version` to
    the running manifest version. US-012 lands the writer (modal).
  - src/content/inject.tsx: new — content script entry. Idempotent
    `mount()` (id=`clipcv-floating-host` guard) attaches a shadow-DOM
    root, injects the Tailwind stylesheet as a `<style>` text node
    (built-time-processed via `?inline`), and createRoot-mounts
    `<FloatingButton />` inside the shadow root. Mounts on
    `DOMContentLoaded` if document is still loading, otherwise inline.
  - src/content/FloatingButton.tsx: new — React component. Reads the
    disclaimer gate on mount + subscribes to chrome.storage.onChanged
    so acceptance flips visibility live. Click handler dispatches
    `{kind:'capture-request', url:window.location.href,
    pageTitle:document.title}` via chrome.runtime.sendMessage and
    surfaces a transient sending → sent/error label.
  - src/content/inject.css: new — `@import 'tailwindcss';` + `:host`
    reset (all:initial + system font stack) so host-page CSS cannot
    inherit through the shadow boundary.
  - src/background/service-worker.ts: register a stateless
    `chrome.runtime.onMessage` handler that uses the typed guard:
    unknown kinds → `{kind:'error', reason:'unknown_kind'}`;
    capture-request → debug-log + `{kind:'capture-request-ack',
    receivedAt}`. Full pipeline implementation lands in US-005+.
- Verification commands run:
  - `npm run typecheck` → OK (tsc --noEmit clean under strict +
    exactOptionalPropertyTypes + noUncheckedIndexedAccess).
  - `npm run build` → dist/ produced:
    - dist/manifest.json with `content_scripts:[{matches:['<all_urls>'],
      js:['assets/inject.tsx-loader-*.js'], run_at:'document_idle',
      all_frames:false}]` and `web_accessible_resources` covering the
      content bundle + jsx-runtime chunk.
    - dist/assets/inject.tsx-loader-*.js (0.34 KB) — CRXJS dynamic-
      import loader; resolves the real bundle via
      chrome.runtime.getURL.
    - dist/assets/inject.tsx-*.js (13.34 KB) — content script bundle
      with the Tailwind v4 utility CSS embedded as a string (no
      separate inject*.css file emitted, isolation guaranteed).
    - dist/assets/service-worker.ts-*.js (0.61 KB) — onMessage handler
      bundled; references onMessage / capture-request /
      capture-request-ack / unknown_kind.
  - Programmatic AC verifier (scripts/verify-us-004.mjs, removed after
    run) → 26/26 checks PASS:
    1) manifest.content_scripts present
    2–5) matches=<all_urls> / js path exists / run_at=document_idle /
         all_frames=false
    6–8) loader uses dynamic import + chrome.runtime.getURL + no eval
    9) web_accessible_resources includes the content bundle
    10) bundle calls attachShadow
    11) bundle uses host id 'clipcv-floating-host'
    12) idempotency: getElementById guard + host id present
    13) bundle reads 'disclaimer_acceptance' storage key
    14) bundle dispatches kind:'capture-request'
    15) bundle reads window.location.href
    16) bundle calls chrome.runtime.sendMessage
    17) no separate inject*.css emitted (Tailwind inlined)
    18) bundle includes Tailwind utilities right-4 + bottom-4
    19) no eval( in bundle
    20) no new Function( in bundle
    21) SW loader resolves to a real bundle
    22) SW bundle has onMessage listener
    23) SW bundle handles capture-request kind
    24) SW bundle returns capture-request-ack
    25) SW bundle rejects unknown kinds with unknown_kind error
    26) §6.4 forbidden-copy grep over new src files clean
  - Forbidden-pattern grep for §6.4 downstream copy
    (ats|crm|recruiter|recruiting|matching|workflow|optitalent) over
    src/ → 0 matches.
  - Browser unpacked load: same Chrome 137+ CLI limitation as US-002
    and US-003 (--load-extension blocked from CLI without policy /
    dev-mode). Structural checks above cover every observable AC
    condition; user-side reload in chrome://extensions is the manual
    verifier for the floating button's visual placement.
- Codebase patterns established (promoted to top of progress.txt):
  - Cross-context messaging contract: discriminated union + type
    guard in src/lib/messaging/messages.ts. Receivers MUST extend
    `isClipcvMessage` before handling new kinds; the SW rejects
    unknown kinds with `{kind:'error', reason:'unknown_kind'}`.
  - Content script structure: entry `src/content/inject.tsx`
    (idempotent shadow-DOM mount) + component `FloatingButton.tsx` +
    stylesheet `inject.css` imported with `?inline` and injected as a
    `<style>` node into the shadow root. Tailwind utility classes work
    inside the shadow root because the processed stylesheet ships
    embedded in the JS bundle as a string.
  - Idempotency convention: stable host element id
    (`clipcv-floating-host`) + `getElementById` guard before insert.
    SPA-navigation safe.
  - Shadow root reset: host element gets `style.all = 'initial'` and
    the stylesheet declares `:host { all: initial }` so neither
    direction of style bleed is possible.
  - Disclaimer gate stub: src/lib/storage/disclaimer-store.ts reads
    `chrome.storage.local.disclaimer_acceptance` keyed on
    `manifest.version`. US-012 writes it; until then the floating
    button is hidden by default. Subscribers use
    `chrome.storage.onChanged` to react live.
  - Service-worker message dispatch: stateless `onMessage` handler
    that returns `false` (synchronous sendResponse) — never holds the
    listener channel open across SW idle.
- Notes / surprises:
  - Prior iteration committed US-002 but left US-003 source files
    untracked (manifest + prd.json + progress.txt were modified but
    the new src/lib/* and src/options/* trees never landed). This
    iteration committed the pending US-003 work as its own commit
    before starting US-004 to restore the per-story-commit invariant.
  - `?inline` CSS import: Tailwind v4 + @tailwindcss/vite respects
    Vite's `?inline` query suffix and emits the post-processed CSS as
    a string into the JS bundle (no separate inject*.css file in
    dist). Required for shadow-DOM isolation — a normal import would
    have CRXJS either inject the stylesheet into the host page <head>
    or attach it via the manifest's content_scripts.css, both of
    which break the isolation invariant.
  - CRXJS auto-emits `web_accessible_resources` for the content
    bundle when the entry uses dynamic import (loader → bundle). This
    is expected and required for the loader's
    `chrome.runtime.getURL(...)` indirection to succeed.

---

## 2026-04-30 — US-005 — Capture viewport screenshot + visible DOM HTML

- Files changed:
  - src/manifest.json: add `host_permissions: ["<all_urls>"]` —
    required for chrome.tabs.captureVisibleTab on arbitrary tabs;
    activeTab cannot be granted via content-script floating-button
    gestures (only toolbar action / context menu / keyboard shortcut).
  - src/lib/messaging/messages.ts: replace the placeholder
    `capture-request-ack` response with the real `capture-screenshot`
    variant carrying `screenshot_b64` + `captured_at`. Expand
    `ClipcvErrorReason` with `screenshot_failed` + `no_active_tab`
    (the SW emits the former on captureVisibleTab failure, e.g. when
    the active tab is a chrome:// page).
  - src/lib/capture/dom-serialize.ts: new — `serializeVisibleDom()`
    clones document.body and walks with TreeWalker(SHOW_ELEMENT) to
    detach SCRIPT / STYLE / NOSCRIPT subtrees and any element with
    `aria-hidden="true"`. Returns post-prune innerHTML. Pure DOM
    logic — runs in the content script.
  - src/lib/capture/payload.ts: new — `CapturePayload` type +
    `applyPayloadSizeCap(payload, 1_000_000)` cap with UTF-8 byte
    measurement via TextEncoder. Over the cap, drops `html` only;
    screenshot is preserved as the primary vision signal.
  - src/lib/capture/screenshot.ts: new — service-worker-only.
    `captureAndDownsample(windowId?, maxWidth=1280)` calls
    chrome.tabs.captureVisibleTab → fetch dataURL → createImageBitmap
    → if width > maxWidth, OffscreenCanvas downsample +
    FileReader.readAsDataURL. All four APIs are SW-available; no DOM.
    bitmap.close() after use.
  - src/background/service-worker.ts: wire the async capture-request
    handler. Listener returns `true` for capture-request branches
    (keeps message channel open) and `false` for unknown_kind. Async
    handler is wrapped in `void handleCaptureRequest(...)` so the
    listener returns synchronously while the SW continues working.
    On error, sendResponse with `{kind:'error', reason:'screenshot_failed',
    detail}`.
  - src/content/capture.ts: new — `requestCapture()` orchestrates
    content-script side: send capture-request → await
    capture-screenshot → serialize visible DOM locally → compose
    `CapturePayload` → applyPayloadSizeCap. Single entry point UI
    components call.
  - src/content/FloatingButton.tsx: hook click handler to
    `requestCapture()` — replaces the US-004 ack stub. On success,
    debug-log a payload summary (page_url, captured_at, html_bytes,
    screenshot_bytes); the full LLM/render/download pipeline lands in
    US-008..US-011.
- Verification commands run:
  - `npm run typecheck` → OK (tsc --noEmit clean under strict +
    exactOptionalPropertyTypes + noUncheckedIndexedAccess).
  - `npm run build` → dist/ produced:
    - dist/manifest.json (1.20 KB) with
      `host_permissions:["<all_urls>"]` alongside
      `permissions:["storage"]`.
    - dist/assets/service-worker.ts-*.js (1.60 KB) with
      captureVisibleTab + createImageBitmap + OffscreenCanvas +
      FileReader + capture-screenshot kind + screenshot_failed reason
      + 1280 px default.
    - dist/assets/inject.tsx-*.js (14.45 KB) with cloneNode +
      createTreeWalker + SCRIPT/STYLE/NOSCRIPT/aria-hidden filters +
      1_000_000 byte cap + TextEncoder + capture-request /
      capture-screenshot kinds + page_url/screenshot_b64 payload
      shape.
    - jsx-runtime / popup / options bundles unchanged from US-004.
  - Programmatic AC verifier (scripts/verify-us-005.mjs, removed
    after run) → 33/33 checks PASS:
    1) manifest.json exists
    2) manifest.host_permissions includes `<all_urls>`
    3) manifest.permissions still includes "storage"
    4) manifest.background.service_worker module-typed
    5–8) SW loader exists + imports the bundled SW asset
    9) SW bundle calls chrome.tabs.captureVisibleTab
    10) SW bundle uses createImageBitmap
    11) SW bundle uses OffscreenCanvas
    12) SW bundle uses FileReader
    13) SW bundle returns capture-screenshot kind
    14) SW bundle returns screenshot_failed on error
    15) SW bundle still rejects unknown_kind
    16) SW bundle stamps captured_at
    17) SW bundle defaults 1280 px max width
    18) SW bundle keeps async channel open (async true / sync false
        branches both present in minified ternary)
    19) content bundle exists
    20) content bundle sends capture-request
    21) content bundle reads capture-screenshot response
    22) content bundle clones document.body
    23) content bundle creates a TreeWalker
    24) content bundle filters SCRIPT/STYLE/NOSCRIPT
    25) content bundle filters aria-hidden
    26) content bundle includes 1 MB cap (1_000_000 / 1e6 / 1000000)
    27) content bundle uses TextEncoder
    28) content bundle constructs page_url + screenshot_b64 shape
    29) no eval(...) in any dist asset
    30) no new Function(...) in any dist asset
    31) §6.4 forbidden-copy grep over new src files clean
    32) small payload preserved by cap
    33) oversized payload drops html and keeps screenshot
  - Forbidden-pattern grep for §6.4 downstream copy
    (ats|crm|recruiter|recruiting|matching|workflow|optitalent) over
    src/lib/capture + src/content/capture.ts +
    src/background/service-worker.ts → 0 matches.
  - Browser unpacked load: same Chrome 137+ CLI limitation as US-002
    through US-004 (--load-extension blocked from CLI without policy
    / dev-mode). Structural checks above cover every observable AC
    condition; user manually loads dist/ in chrome://extensions to
    confirm the floating button now triggers a screenshot + DOM
    serialize round-trip and the debug payload summary appears in
    devtools console.
- Codebase patterns established (promoted to top of progress.txt):
  - Service-worker async dispatch: listener returns `true` only for
    branches that need async sendResponse; the handler MUST call
    sendResponse in every code path including catch (otherwise
    Chrome times out the channel).
  - Screenshot capture pipeline: captureVisibleTab → fetch data URL
    → createImageBitmap → OffscreenCanvas downsample to 1280 px →
    FileReader.readAsDataURL. All four APIs are MV3-SW-available.
  - DOM strip rule: clone body, TreeWalker(SHOW_ELEMENT) collect,
    detach in reverse order. Strips SCRIPT/STYLE/NOSCRIPT +
    aria-hidden="true".
  - Cross-context payload shape: CapturePayload is the single
    contract from capture orchestrator into US-008+ vision-extract.
    1 MB cap drops html only; screenshot preserved as primary signal.
  - Capture orchestration topology: content owns click + DOM
    serialize; SW owns screenshot. Single entry point in
    src/content/capture.ts:requestCapture().
  - Manifest host_permissions: `<all_urls>` required for
    captureVisibleTab; activeTab is not a usable substitute when the
    trigger is a content-script gesture.
- Notes / surprises:
  - The minifier rewrites `return true` / `return false` into a
    ternary `... ? (..., !0) : (..., !1)`. The verifier's
    "channel-stays-open" check has to look for `!0)` and `!1)` in
    the bundle text rather than the literal `return true` source.
  - chrome.tabs.captureVisibleTab fails on chrome://, about:, and
    Chrome Web Store URLs — the try/catch in handleCaptureRequest
    surfaces this as `screenshot_failed` with the underlying error
    message in `detail`. UI toasts in US-011 will surface this user-
    facingly; for now the FloatingButton flips to "Error" state.
  - FileReader.readAsDataURL avoids the
    `String.fromCharCode(...bytes)` chunk-loop trick for large
    blobs — readable and stack-safe.
  - vitest is still not wired up; US-005's AC does not require unit
    tests (it is a UI/extension-surface story per CLAUDE.md §7.3),
    so deferred to US-006 / US-007 / US-008 where unit tests are
    explicit acceptance criteria. Pure-logic round-trip for the
    1 MB cap was verified inline in the one-off node verifier.

---
